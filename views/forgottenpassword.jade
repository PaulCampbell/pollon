extends layout

block content
    section.css-table
        h2 Request a new password

        - if(locals.flashmsg)
            div(class="alert alert-error") #{locals.flashmsg}

        form(name="passwordrequest", action="/request-password", method="post")
          div
            label Email:
            input(name='email')
          div
            input(type="submit", id='register', value="Register")

